{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": ".test-build",
    "rootDir": "src",
    "lib": ["ES2022", "DOM"],
    "types": []
  },
  "include": ["src/**/*.ts"],
  "//": "index.ts and extract.ts bind to the Workers runtime (McpAgent, HTMLRewriter) and cannot run under plain Node. `npm run typecheck` still covers them.",
  "exclude": ["src/index.ts", "src/extract.ts"]
}
